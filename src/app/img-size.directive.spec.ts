import { ImgSizeDirective } from './img-size.directive';

describe('ImgSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgSizeDirective();
    expect(directive).toBeTruthy();
  });
});
